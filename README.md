# inventory
A console-based inventory management system for fruit vendors built using Python and SQL.
The application features two modes—Owner and Customer—where the Owner can add, update, view, and delete inventory records, and the Customer can browse items, manage a cart, and generate bills with database integration.
